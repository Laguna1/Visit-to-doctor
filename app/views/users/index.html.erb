<h1>Users list</h1>
<ul>
<% @users.each do |user| %>
  <li><%= user.fullname %> || <%= user.mobile_no %> || <%= show_category(user) %></li>
<% end %>
</ul>

<%= link_to "Sign out", destroy_user_session_path, method: :delete %>